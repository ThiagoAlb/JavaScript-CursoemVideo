function soma(n1, n2) {
    return n1 + n2
}

//n1=0, n2=0 Isso serve para caso um dos parâmetros não seja passado. O que não foi passado vai assumir o valor de 0 ou outro que eu atribuir

console.log(soma(2, 5))